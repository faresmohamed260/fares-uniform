name: Commit generated D-062 corporate asset

on:
  push:
    branches:
      - phase-10/public-site-productionization
    paths:
      - "docs/ui/homepage/assets/.generated-home-corporate/**"
      - ".github/workflows/commit-generated-home-corporate.yml"

permissions:
  contents: write

jobs:
  reconstruct:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: Reconstruct verified corporate asset
        run: |
          set -euo pipefail
          STAGE="docs/ui/homepage/assets/.generated-home-corporate"
          TARGET="prototype/public-site-design-authority/public/generated/home-industries-corporate.webp"
          EXPECTED="3a9fe5915143492a9c7a465e7ca947bf3297745ab120fc6614d7e7f00a2abe45"
          mkdir -p "$(dirname "$TARGET")"
          test "$(find "$STAGE" -maxdepth 1 -type f -name 'part-*.b64' | wc -l)" -eq 1
          joined="$(mktemp)"
          trap 'rm -f "$joined"' EXIT
          cat "$STAGE"/part-*.b64 > "$joined"
          test "$(wc -c < "$joined")" -eq 6084
          base64 --decode "$joined" > "$TARGET"
          test "$(sha256sum "$TARGET" | awk '{print $1}')" = "$EXPECTED"
          file "$TARGET" | grep -Eq 'Web/P image|WebP'
          file "$TARGET" | grep -Eq '159x137|159 x 137'
          rm -rf "$STAGE"
          rm -f ".github/workflows/commit-generated-home-corporate.yml"
      - name: Commit reconstructed binary
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "4189822+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "design: commit generated D-062 corporate media"
          git push origin HEAD:phase-10/public-site-productionization
