name: Commit generated D-062 healthcare asset

on:
  push:
    branches:
      - phase-10/public-site-productionization
    paths:
      - "docs/ui/homepage/assets/.generated-home-healthcare/**"
      - ".github/workflows/commit-generated-home-healthcare.yml"

permissions:
  contents: write

jobs:
  reconstruct:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: Reconstruct verified healthcare asset
        run: |
          set -euo pipefail
          STAGE="docs/ui/homepage/assets/.generated-home-healthcare"
          TARGET="prototype/public-site-design-authority/public/generated/home-industries-healthcare.webp"
          EXPECTED="5dbe74ffbd8ce363a4e0acaaed170690b43e1a87d60bd7206c4eec4e21a1a6ee"
          mkdir -p "$(dirname "$TARGET")"
          test "$(find "$STAGE" -maxdepth 1 -type f -name 'part-*.b64' | wc -l)" -eq 1
          joined="$(mktemp)"
          trap 'rm -f "$joined"' EXIT
          cat "$STAGE"/part-*.b64 > "$joined"
          test "$(wc -c < "$joined")" -eq 5264
          base64 --decode "$joined" > "$TARGET"
          test "$(sha256sum "$TARGET" | awk '{print $1}')" = "$EXPECTED"
          file "$TARGET" | grep -Eq 'Web/P image|WebP'
          file "$TARGET" | grep -Eq '159x137|159 x 137'
          rm -rf "$STAGE"
          rm -f ".github/workflows/commit-generated-home-healthcare.yml"
      - name: Commit reconstructed binary
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "4189822+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "design: commit generated D-062 healthcare media"
          git push origin HEAD:phase-10/public-site-productionization
